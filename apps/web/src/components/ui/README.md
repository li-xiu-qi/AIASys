# UI Components

本目录下的组件是通过 [shadcn/ui](https://ui.shadcn.com/) 自动生成的。

## 说明

- **请勿直接在此目录下手动修改组件逻辑**，除非确有定制化需求。
- 建议通过 `npx shadcn-ui@latest add [component-name]` 来添加新组件。
- 样式主要通过 Tailwind CSS 处理，配置文件位于根目录的 `tailwind.config.js`。
