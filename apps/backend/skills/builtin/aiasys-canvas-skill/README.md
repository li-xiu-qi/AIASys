# AIASys Canvas 编辑

操作 JSON Canvas 格式的 `.canvas` 文件。支持创建和修改思维导图、流程图、白板笔记、概念图等可视化结构。

## 适用场景

- 画思维导图、流程图、概念图
- 把文本内容转化为可视化的节点-边结构
- 读取和修改已有的 `.canvas` 文件

## 使用方法

1. 确保当前工作区已启用此 skill
2. 使用 Agent 工具面板中的 `ReadCanvas`、`WriteCanvas`、`BatchCanvasOperations` 进行读写和批量修改
3. 未知扩展字段只做透传，不丢失数据
